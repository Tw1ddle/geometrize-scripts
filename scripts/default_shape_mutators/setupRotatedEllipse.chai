def setupRotatedEllipse(shape) {
    shape.m_x = randomInRange(0, xMax);
    shape.m_y = randomInRange(0, yMax);
    shape.m_rx = randomInRange(1, 32);
    shape.m_ry = randomInRange(1, 32);
    shape.m_angle = randomInRange(0, 360);
}
